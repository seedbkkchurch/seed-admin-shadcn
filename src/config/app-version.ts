// package.json's version, baked in at build time via vite.config.ts `define`
// (grill-me follow-up, 2026-08-23). Shown on the sign-in page and in the
// sidebar footer so support can tell which build a user is on.
export const APP_VERSION = __APP_VERSION__;
